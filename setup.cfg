[flake8]
max-line-length = 120
max-complexity = 20
exclude = 
    = tests
    = scripts

[options]
package_dir=
    = ./src
packages = find_namespace:

[options.packages.find]
where = ./src
exclude = tests*
